# Copyright 2026 The IREE Authors
#
# Licensed under the Apache License v2.0 with LLVM Exceptions.
# See https://llvm.org/LICENSE.txt for license information.
# SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception

load("//loom/build_tools/bazel:loom_check.bzl", "loom_check_test", "loom_check_test_suite")

package(
    default_visibility = ["//visibility:public"],
    licenses = ["notice"],  # Apache 2.0
)

loom_check_test_suite(
    name = "core_test",
    srcs = [
        "emit.loom-test",
        "llvmir/intrinsics.loom-test",
        "llvmir/lowering_errors.loom-test",
        "llvmir/memory.loom-test",
        "llvmir/packed_dot.loom-test",
        "llvmir/scalar_index.loom-test",
        "llvmir/toolchain.loom-test",
        "llvmir/vector_core.loom-test",
        "low_allocation.loom-test",
        "low_allocation_materialization.loom-test",
        "low_packet.loom-test",
        "low_schedule.loom-test",
        "low_verify.loom-test",
        "pass.loom-test",
        "requires.loom-test",
        "roundtrip.loom-test",
        "verify.loom-test",
    ],
)

loom_check_test(
    name = "amdgpu_hal_run",
    src = "amdgpu_hal_run.loom-test",
    data = ["//loom/src/loom/tools/iree-run-loom:iree-run-loom-amdgpu"],
    env = {
        "LOOM_CHECK_IREE_RUN_LOOM": "$(rootpath //loom/src/loom/tools/iree-run-loom:iree-run-loom-amdgpu)",
    },
)

loom_check_test(
    name = "run",
    src = "run.loom-test",
    data = ["//loom/src/loom/tools/iree-run-loom"],
    env = {
        "LOOM_CHECK_IREE_RUN_LOOM": "$(rootpath //loom/src/loom/tools/iree-run-loom)",
    },
)

test_suite(
    name = "test",
    tests = [
        ":amdgpu_hal_run",
        ":core_test",
        ":run",
    ],
)
