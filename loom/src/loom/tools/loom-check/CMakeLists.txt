################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/src/loom/tools/loom-check/BUILD.bazel                                   #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

loom_cc_library(
  NAME
    check
  HDRS
    "check.h"
  SRCS
    "check.c"
  DEPS
    iree::base
    iree::base::internal::arena
    loom::error::error_defs
  TESTONLY
  PUBLIC
)

loom_cc_library(
  NAME
    execute
  HDRS
    "diagnostics.h"
    "execute.h"
    "low_emit.h"
    "requirements.h"
  SRCS
    "diagnostics.c"
    "emit.c"
    "execute.c"
    "low_emit.c"
    "requirements.c"
    "roundtrip.c"
    "verify.c"
  DEPS
    ::check
    ::report
    ::update
    iree::base
    iree::base::internal::arena
    iree::io::file_handle
    iree::io::stream
    loom::analysis::liveness
    loom::analysis::liveness_json
    loom::codegen::low::allocation
    loom::codegen::low::allocation_json
    loom::codegen::low::descriptors
    loom::codegen::low::descriptors_manifest
    loom::codegen::low::frame
    loom::codegen::low::function
    loom::codegen::low::lower
    loom::codegen::low::packet_json
    loom::codegen::low::pass_environment
    loom::codegen::low::schedule
    loom::codegen::low::target_binding
    loom::codegen::low::text_asm
    loom::codegen::low::verify
    loom::error::diagnostic
    loom::error::emitter
    loom::error::error_defs
    loom::error::json_sink
    loom::error::renderer
    loom::format::text::parser
    loom::format::text::printer
    loom::ir
    loom::ops::func
    loom::ops::kernel
    loom::ops::low
    loom::ops::op_defs
    loom::ops::target
    loom::pass::builtin_registry
    loom::pass::pipeline
    loom::pass::registry
    loom::pass::tooling
    loom::target::compile_report_format
    loom::target::entry_selection
    loom::target::legalization
    loom::target::low_descriptor_registry
    loom::target::low_descriptor_registry_manifest
    loom::target::low_legality
    loom::target::low_packet_diagnostics
    loom::target::math_policy
    loom::target::predicate
    loom::target::types
    loom::testing::diff
    loom::tooling::compile::pipeline
    loom::tools::loom-format::convert
    loom::util::json
    loom::util::stream
    loom::verify
  TESTONLY
  PUBLIC
)

loom_cc_library(
  NAME
    test_util
  HDRS
    "test_util.h"
  SRCS
    "test_util.cc"
  DEPS
    ::check
    ::execute
    ::report
    iree::base
    iree::base::internal::arena
    loom::ir
    loom::ops::op_registry
  TESTONLY
  PUBLIC
)

loom_cc_library(
  NAME
    report
  HDRS
    "report.h"
  SRCS
    "report.c"
  DEPS
    ::check
    iree::base
    iree::base::internal::arena
  TESTONLY
  PUBLIC
)

loom_cc_test(
  NAME
    check_test
  SRCS
    "check_test.cc"
  DEPS
    ::check
    iree::base
    iree::base::internal::arena
    iree::testing::gtest
    iree::testing::gtest_main
)

loom_cc_test(
  NAME
    execute_test
  SRCS
    "execute_test.cc"
  DEPS
    ::check
    ::execute
    ::report
    iree::base
    iree::base::internal::arena
    iree::testing::gtest
    iree::testing::gtest_main
    loom::error::error_defs
    loom::ir
    loom::target::low_descriptor_registry_core_test
    loom::target::provider
    loom::target::test::lower
    loom::testing::context
)

loom_cc_library(
  NAME
    update
  HDRS
    "update.h"
  SRCS
    "update.c"
  DEPS
    ::check
    iree::base
  TESTONLY
  PUBLIC
)

loom_cc_library(
  NAME
    template_sync
  HDRS
    "template_sync.h"
  SRCS
    "template_sync.c"
  DEPS
    ::check
    iree::base
    iree::base::internal::arena
    loom::format::text::parser
    loom::ir
    loom::ops::op_defs
  TESTONLY
  PUBLIC
)

loom_cc_test(
  NAME
    update_test
  SRCS
    "update_test.cc"
  DEPS
    ::check
    ::update
    iree::base
    iree::base::internal::arena
    iree::testing::gtest
    iree::testing::gtest_main
)

loom_cc_test(
  NAME
    template_sync_test
  SRCS
    "template_sync_test.cc"
  DEPS
    ::check
    ::template_sync
    iree::base
    iree::base::internal::arena
    iree::testing::gtest
    iree::testing::gtest_main
    loom::ir
    loom::ops::func
    loom::ops::index
    loom::ops::kernel
    loom::ops::scalar
)

loom_cc_library(
  NAME
    json_output
  HDRS
    "json_output.h"
  SRCS
    "json_output.c"
  DEPS
    ::check
    ::execute
    ::report
    ::update
    iree::base
    loom::util::json
    loom::util::stream
  TESTONLY
  PUBLIC
)

loom_cc_library(
  NAME
    output
  HDRS
    "output.h"
  SRCS
    "output.c"
  DEPS
    ::check
    ::execute
    iree::base
    iree::base::core_headers
  TESTONLY
  PUBLIC
)

loom_cc_library(
  NAME
    file
  HDRS
    "file.h"
  SRCS
    "file.c"
  DEPS
    ::check
    ::execute
    ::json_output
    ::output
    ::report
    ::template_sync
    ::update
    iree::base
    iree::base::internal::arena
    iree::io::file_handle
    loom::ir
    loom::tooling::io::file
    loom::util::stream
  TESTONLY
  PUBLIC
)

loom_cc_library(
  NAME
    main
  HDRS
    "main.h"
  SRCS
    "main.c"
  DEPS
    ::execute
    ::file
    ::json_output
    ::output
    iree::base
    iree::base::internal::arena
    iree::base::tooling::flags
    loom::ops::op_registry
  TESTONLY
  PUBLIC
)

loom_cc_library(
  NAME
    provider
  HDRS
    "provider.h"
  SRCS
    "provider.c"
  DEPS
    ::main
    iree::base
    loom::codegen::low::lower
    loom::ir
    loom::target::math_policy
    loom::target::provider
  TESTONLY
  PUBLIC
)

loom_cc_test(
  NAME
    json_output_test
  SRCS
    "json_output_test.cc"
  DEPS
    ::check
    ::execute
    ::json_output
    ::report
    iree::base
    iree::base::internal::arena
    iree::testing::gtest
    iree::testing::gtest_main
    loom::util::stream
)

set(_loom_check_platform_copts "")
if(LOOM_TARGET_AMDGPU)
  list(APPEND _loom_check_platform_copts "-DLOOM_CHECK_HAVE_TARGET_AMDGPU=1")
endif()
if(LOOM_TARGET_IREEVM)
  list(APPEND _loom_check_platform_copts "-DLOOM_CHECK_HAVE_TARGET_IREEVM=1")
endif()
if(LOOM_EMIT_LLVMIR)
  list(APPEND _loom_check_platform_copts "-DLOOM_CHECK_HAVE_EMIT_LLVMIR=1")
endif()
if(LOOM_TARGET_SPIRV)
  list(APPEND _loom_check_platform_copts "-DLOOM_CHECK_HAVE_TARGET_SPIRV=1")
endif()
if(LOOM_EMIT_SPIRV)
  list(APPEND _loom_check_platform_copts "-DLOOM_CHECK_HAVE_EMIT_SPIRV=1")
endif()
if(LOOM_TARGET_WASM)
  list(APPEND _loom_check_platform_copts "-DLOOM_CHECK_HAVE_TARGET_WASM=1")
endif()
if(LOOM_EMIT_WASM)
  list(APPEND _loom_check_platform_copts "-DLOOM_CHECK_HAVE_EMIT_WASM=1")
endif()
if(LOOM_TARGET_X86)
  list(APPEND _loom_check_platform_copts "-DLOOM_CHECK_HAVE_TARGET_X86=1")
endif()
set(_loom_check_platform_deps "")
if(LOOM_TARGET_AMDGPU)
  list(APPEND _loom_check_platform_deps loom::target::arch::amdgpu::check_provider)
endif()
if(LOOM_TARGET_IREEVM)
  list(APPEND _loom_check_platform_deps loom::target::arch::ireevm::check_provider)
endif()
if(LOOM_EMIT_LLVMIR)
  list(APPEND _loom_check_platform_deps loom::target::emit::llvmir::check_provider)
endif()
if(LOOM_TARGET_SPIRV)
  list(APPEND _loom_check_platform_deps loom::target::arch::spirv::check_provider)
endif()
if(LOOM_EMIT_SPIRV)
  list(APPEND _loom_check_platform_deps loom::target::emit::spirv::check_provider)
endif()
if(LOOM_TARGET_WASM)
  list(APPEND _loom_check_platform_deps loom::target::arch::wasm::check_provider)
endif()
if(LOOM_EMIT_WASM)
  list(APPEND _loom_check_platform_deps loom::target::emit::wasm::check_provider)
endif()
if(LOOM_TARGET_X86)
  list(APPEND _loom_check_platform_deps loom::target::arch::x86::check_provider)
endif()
loom_cc_binary(
  NAME
    loom-check
  SRCS
    "loom-check.c"
  COPTS
    ${_loom_check_platform_copts}
  DEPS
    ::provider
    ${_loom_check_platform_deps}
  TESTONLY
)

loom_cc_binary(
  NAME
    loom-check-test
  SRCS
    "loom-check-test.c"
  DEPS
    ::execute
    ::main
    iree::base
    loom::codegen::low::allocation
    loom::codegen::low::packet_hazard_plan
    loom::codegen::low::packet_hazard_plan_json
    loom::codegen::low::packet_progress
    loom::ir
    loom::ops::test
    loom::pass::test::registry
    loom::target::low_descriptor_registry_core_test
    loom::target::test::lower
  TESTONLY
)

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###

set(_LOOM_CHECK_ALL_PROVIDERS amdgpu ireevm llvmir wasm x86)
set(IREE_LOOM_CHECK_PROVIDERS "${_LOOM_CHECK_ALL_PROVIDERS}" CACHE STRING
  "Semicolon-separated list of target providers linked into loom-check."
)
foreach(_LOOM_CHECK_PROVIDER ${IREE_LOOM_CHECK_PROVIDERS})
  if(NOT _LOOM_CHECK_PROVIDER IN_LIST _LOOM_CHECK_ALL_PROVIDERS)
    message(FATAL_ERROR
      "Unknown loom-check provider '${_LOOM_CHECK_PROVIDER}'. "
      "Expected one of: ${_LOOM_CHECK_ALL_PROVIDERS}"
    )
  endif()
endforeach()

iree_package_name(_LOOM_CHECK_PACKAGE_NAME)
set(_LOOM_CHECK_TARGET "${_LOOM_CHECK_PACKAGE_NAME}_loom-check")

function(_loom_check_enable_provider _target _definition)
  target_compile_definitions(${_target} PRIVATE
    ${_definition}=1
  )
  target_link_libraries(${_target} PRIVATE
    ${ARGN}
  )
endfunction()

if("amdgpu" IN_LIST IREE_LOOM_CHECK_PROVIDERS)
  _loom_check_enable_provider(
    ${_LOOM_CHECK_TARGET}
    LOOM_CHECK_HAVE_AMDGPU
    loom::target::arch::amdgpu::check_provider
  )
endif()

if("ireevm" IN_LIST IREE_LOOM_CHECK_PROVIDERS)
  _loom_check_enable_provider(
    ${_LOOM_CHECK_TARGET}
    LOOM_CHECK_HAVE_IREEVM
    loom::target::arch::ireevm::check_provider
  )
endif()

if("llvmir" IN_LIST IREE_LOOM_CHECK_PROVIDERS)
  _loom_check_enable_provider(
    ${_LOOM_CHECK_TARGET}
    LOOM_CHECK_HAVE_LLVMIR
    loom::target::emit::llvmir::check_provider
  )
endif()

if("wasm" IN_LIST IREE_LOOM_CHECK_PROVIDERS)
  _loom_check_enable_provider(
    ${_LOOM_CHECK_TARGET}
    LOOM_CHECK_HAVE_WASM
    loom::target::arch::wasm::check_provider
    loom::target::emit::wasm::check_provider
  )
endif()

if("x86" IN_LIST IREE_LOOM_CHECK_PROVIDERS)
  _loom_check_enable_provider(
    ${_LOOM_CHECK_TARGET}
    LOOM_CHECK_HAVE_X86
    loom::target::arch::x86::check_provider
  )
endif()

unset(_LOOM_CHECK_TARGET)
unset(_LOOM_CHECK_PACKAGE_NAME)
unset(_LOOM_CHECK_ALL_PROVIDERS)
