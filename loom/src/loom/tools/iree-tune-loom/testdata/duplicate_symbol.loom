func.def @duplicate_symbol() {
  func.return
}

check.case @duplicate_symbol {
  check.return
}

check.benchmark @duplicate_symbol_latency case(@duplicate_symbol) attrs({measure = "case_end_to_end"})
