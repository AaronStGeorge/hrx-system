################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/src/loom/tools/iree-test-loom/BUILD.bazel                            #
#                                                                              #
# Use iree_cmake_extra_content from loom/build_tools/bazel/build_defs.bzl to add arbitrary   #
# CMake-only content.                                                          #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

loom_cc_library(
  NAME
    main
  HDRS
    "main.h"
  SRCS
    "main.c"
  DEPS
    iree::base
    iree::base::tooling::flags
    iree::io::file_handle
    loom::src::loom::ops::op_registry
    loom::src::loom::tooling::execution::session
    loom::src::loom::tooling::io::file
    loom::src::loom::tooling::testbench::executor
    loom::src::loom::util::stream
  PUBLIC
)

loom_cc_binary(
  NAME
    iree-test-loom
  SRCS
    "iree-test-loom.c"
  COPTS

  DEPS
    ::main
    iree::base
    loom::src::loom::tooling::execution::execution_provider
)

iree_lit_test_suite(
  NAME
    lit_test
  SRCS
    "iree-test-loom_test.txt"
  TOOLS
    ::iree-test-loom
    FileCheck
  DATA
    testdata::scalar_cases.loom
  LABELS
    "hostonly"
)

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###

set(_IREE_TEST_LOOM_ALL_PROVIDERS amdgpu ireevm)
set(IREE_TEST_LOOM_PROVIDERS "${_IREE_TEST_LOOM_ALL_PROVIDERS}" CACHE STRING
  "Semicolon-separated list of execution providers linked into iree-test-loom."
)
foreach(_IREE_TEST_LOOM_PROVIDER ${IREE_TEST_LOOM_PROVIDERS})
  if(NOT _IREE_TEST_LOOM_PROVIDER IN_LIST _IREE_TEST_LOOM_ALL_PROVIDERS)
    message(FATAL_ERROR
      "Unknown iree-test-loom provider '${_IREE_TEST_LOOM_PROVIDER}'. "
      "Expected one of: ${_IREE_TEST_LOOM_ALL_PROVIDERS}"
    )
  endif()
endforeach()

iree_package_name(_IREE_TEST_LOOM_PACKAGE_NAME)
set(_IREE_TEST_LOOM_TARGET "${_IREE_TEST_LOOM_PACKAGE_NAME}_iree-test-loom")

function(_iree_test_loom_enable_provider _target _definition _library)
  target_compile_definitions(${_target} PRIVATE
    ${_definition}=1
  )
  target_link_libraries(${_target} PRIVATE
    ${_library}
  )
endfunction()

if("amdgpu" IN_LIST IREE_TEST_LOOM_PROVIDERS)
  _iree_test_loom_enable_provider(
    ${_IREE_TEST_LOOM_TARGET}
    IREE_TEST_LOOM_HAVE_AMDGPU
    loom::src::loom::target::arch::amdgpu::provider
  )
endif()

if("ireevm" IN_LIST IREE_TEST_LOOM_PROVIDERS)
  _iree_test_loom_enable_provider(
    ${_IREE_TEST_LOOM_TARGET}
    IREE_TEST_LOOM_HAVE_IREEVM
    loom::src::loom::tooling::execution::ireevm::provider
  )
endif()

unset(_IREE_TEST_LOOM_TARGET)
unset(_IREE_TEST_LOOM_PACKAGE_NAME)
unset(_IREE_TEST_LOOM_ALL_PROVIDERS)
