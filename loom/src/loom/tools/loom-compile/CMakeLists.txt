################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/src/loom/tools/loom-compile/BUILD.bazel                              #
#                                                                              #
# Use iree_cmake_extra_content from loom/build_tools/bazel/build_defs.bzl to add arbitrary   #
# CMake-only content.                                                          #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

loom_cc_binary(
  NAME
    loom-compile
  SRCS
    "loom-compile.c"
  COPTS

  DEPS
    iree::base
    iree::base::tooling::flags
    iree::io::file_handle
    loom::src::loom::error::diagnostic
    loom::src::loom::ops::op_registry
    loom::src::loom::tooling::compile::pipeline
    loom::src::loom::tooling::config
    loom::src::loom::tooling::execution::compile_options
    loom::src::loom::tooling::execution::compile_report_capture
    loom::src::loom::tooling::execution::execution_provider
    loom::src::loom::tooling::execution::hal::artifact
    loom::src::loom::tooling::execution::hal::candidate
    loom::src::loom::tooling::execution::session
    loom::src::loom::tooling::io::file
)

iree_lit_test_suite(
  NAME
    lit_test
  SRCS
    "loom-compile_test.txt"
  TOOLS
    ::loom-compile
    FileCheck
    iree-run-module
  LABELS
    "hostonly"
)

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###

option(LOOM_COMPILE_ENABLE_AMDGPU
  "Link the AMDGPU target provider into loom-compile." ON)
option(LOOM_COMPILE_ENABLE_IREEVM
  "Link the IREEVM execution provider into loom-compile." ON)
option(LOOM_COMPILE_ENABLE_SPIRV
  "Link the SPIR-V target provider into loom-compile." ON)

iree_package_name(_LOOM_COMPILE_PACKAGE_NAME)
set(_LOOM_COMPILE_TARGET "${_LOOM_COMPILE_PACKAGE_NAME}_loom-compile")

function(_loom_compile_enable_provider _target _definition _library)
  target_compile_definitions(${_target} PRIVATE
    ${_definition}=1
  )
  target_link_libraries(${_target} PRIVATE
    ${_library}
  )
endfunction()

if(LOOM_COMPILE_ENABLE_AMDGPU)
  _loom_compile_enable_provider(
    ${_LOOM_COMPILE_TARGET}
    LOOM_COMPILE_HAVE_AMDGPU
    loom::src::loom::target::arch::amdgpu::provider
  )
endif()

if(LOOM_COMPILE_ENABLE_IREEVM)
  _loom_compile_enable_provider(
    ${_LOOM_COMPILE_TARGET}
    LOOM_COMPILE_HAVE_IREEVM
    loom::src::loom::tooling::target::ireevm::candidate
  )
  target_link_libraries(${_LOOM_COMPILE_TARGET} PRIVATE
    loom::src::loom::tooling::target::ireevm::provider
  )
endif()

if(LOOM_COMPILE_ENABLE_SPIRV)
  _loom_compile_enable_provider(
    ${_LOOM_COMPILE_TARGET}
    LOOM_COMPILE_HAVE_SPIRV
    loom::src::loom::target::arch::spirv::provider
  )
  target_link_libraries(${_LOOM_COMPILE_TARGET} PRIVATE
    loom::src::loom::tooling::target::spirv::artifact_provider
  )
endif()

unset(_LOOM_COMPILE_TARGET)
unset(_LOOM_COMPILE_PACKAGE_NAME)
