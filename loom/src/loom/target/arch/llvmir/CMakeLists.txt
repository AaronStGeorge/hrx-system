################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/src/loom/target/arch/llvmir/BUILD.bazel                                 #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(LOOM_TARGET_ARCH_LLVMIR)
loom_cc_library(
  NAME
    math_policy
  HDRS
    "math_policy.h"
  TEXTUAL_HDRS
    "math_policy_tables.inl"
  SRCS
    "math_policy.c"
  DEPS
    iree::base
    loom::target::math_policy
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_LLVMIR)
loom_cc_library(
  NAME
    provider
  HDRS
    "provider.h"
  SRCS
    "provider.c"
  DEPS
    ::math_policy
    iree::base
    loom::ops::llvmir
    loom::target::arch::llvmir::descriptors::low_registry
    loom::target::arch::llvmir::lower
    loom::target::provider
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_LLVMIR)
loom_cc_test(
  NAME
    provider_test
  SRCS
    "provider_test.cc"
  DEPS
    ::provider
    iree::base
    iree::base::internal::arena
    iree::testing::gtest
    iree::testing::gtest_main
    loom::format::text::parser
    loom::ir
    loom::ops::llvmir
    loom::target::arch::llvmir::descriptors::low_registry
    loom::target::arch::llvmir::lower
    loom::target::provider
    loom::testing::module_ptr
  LABELS
    "iree-build-requirement=loom.target.arch.llvmir"
)
endif()

if(LOOM_TARGET_ARCH_LLVMIR)
iree_native_test(
  NAME
    "source_low"
  ARGS
    "${CMAKE_CURRENT_SOURCE_DIR}/test/source_low/source_low.loom-test"
  SRC
    loom::tools::loom-check::loom-check
  LABELS
    "loom-check"
)
endif()

if(LOOM_TARGET_ARCH_LLVMIR)
iree_native_test(
  NAME
    "source_low_diagnostics"
  ARGS
    "${CMAKE_CURRENT_SOURCE_DIR}/test/source_low/source_low_diagnostics.loom-test"
  SRC
    loom::tools::loom-check::loom-check
  LABELS
    "loom-check"
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
