// TEMPLATE: loom/src/loom/test/corpus/source_low/sync_barrier.loom-test
// RUN: emit source-low output=low

amdgpu.target<gfx1100> @target
func.def target(@target) @workgroup_barrier() {
  kernel.barrier {memory_space = workgroup, ordering = acq_rel, scope = workgroup}
  func.return
}

// ----
low.func.def target(@target) abi(hal_kernel) @workgroup_barrier() asm<amdgpu.gfx11.core> {
  s_barrier
  return
}

// ====

amdgpu.target<gfx1100> @target
func.def target(@target) @subgroup_barrier_rejected() {
  // ERROR@+1: BACKEND/001 "AMDGPU source-to-low currently lowers only workgroup-scope barriers"
  kernel.barrier {memory_space = workgroup, ordering = acq_rel, scope = subgroup}
  func.return
}
