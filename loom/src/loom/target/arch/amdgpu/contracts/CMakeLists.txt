################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/src/loom/target/arch/amdgpu/contracts/BUILD.bazel                       #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(LOOM_TARGET_AMDGPU)
loom_target_table_cc_library(
  NAME
    arithmetic
  GENERATOR
    loom::py::loom::gen::amdgpu_contract_fragment
  ARGS
    "--contract-fragment=amdgpu.arithmetic"
  DEPS
    loom::target::contract
)
endif()

if(LOOM_TARGET_AMDGPU)
loom_target_table_cc_library(
  NAME
    arithmetic_lower_rules
  GENERATOR
    loom::py::loom::gen::amdgpu_lower_rule_shard
  ARGS
    "--contract-fragment=amdgpu.arithmetic"
  DEPS
    loom::codegen::low::lower
    loom::ops::index
    loom::ops::scalar
    loom::ops::vector
    loom::target::arch::amdgpu::lower_kinds
    loom::target::arch::amdgpu::lower_materializers
)
endif()

if(LOOM_TARGET_AMDGPU)
loom_target_table_cc_library(
  NAME
    async
  GENERATOR
    loom::py::loom::gen::amdgpu_contract_fragment
  ARGS
    "--contract-fragment=amdgpu.async"
  DEPS
    loom::target::contract
)
endif()

if(LOOM_TARGET_AMDGPU)
loom_target_table_cc_library(
  NAME
    async_lower_rules
  GENERATOR
    loom::py::loom::gen::amdgpu_lower_rule_shard
  ARGS
    "--contract-fragment=amdgpu.async"
  DEPS
    loom::codegen::low::lower
    loom::ops::kernel
)
endif()

if(LOOM_TARGET_AMDGPU)
loom_target_table_cc_library(
  NAME
    buffer
  GENERATOR
    loom::py::loom::gen::amdgpu_contract_fragment
  ARGS
    "--contract-fragment=amdgpu.buffer"
  DEPS
    loom::target::contract
)
endif()

if(LOOM_TARGET_AMDGPU)
loom_target_table_cc_library(
  NAME
    buffer_lower_rules
  GENERATOR
    loom::py::loom::gen::amdgpu_lower_rule_shard
  ARGS
    "--contract-fragment=amdgpu.buffer"
  DEPS
    loom::codegen::low::lower
    loom::ops::buffer
)
endif()

if(LOOM_TARGET_AMDGPU)
loom_target_table_cc_library(
  NAME
    compare
  GENERATOR
    loom::py::loom::gen::amdgpu_contract_fragment
  ARGS
    "--contract-fragment=amdgpu.compare"
  DEPS
    loom::target::contract
)
endif()

if(LOOM_TARGET_AMDGPU)
loom_target_table_cc_library(
  NAME
    compare_lower_rules
  GENERATOR
    loom::py::loom::gen::amdgpu_lower_rule_shard
  ARGS
    "--contract-fragment=amdgpu.compare"
  DEPS
    loom::codegen::low::lower
    loom::ops::index
    loom::ops::scalar
    loom::target::arch::amdgpu::lower_materializers
)
endif()

if(LOOM_TARGET_AMDGPU)
loom_target_table_cc_library(
  NAME
    dot
  GENERATOR
    loom::py::loom::gen::amdgpu_contract_fragment
  ARGS
    "--contract-fragment=amdgpu.dot"
  DEPS
    loom::target::contract
)
endif()

if(LOOM_TARGET_AMDGPU)
loom_target_table_cc_library(
  NAME
    dot_lower_rules
  GENERATOR
    loom::py::loom::gen::amdgpu_lower_rule_shard
  ARGS
    "--contract-fragment=amdgpu.dot"
  DEPS
    loom::codegen::low::lower
    loom::ops::vector
    loom::target::arch::amdgpu::lower_kinds
)
endif()

if(LOOM_TARGET_AMDGPU)
loom_target_table_cc_library(
  NAME
    integer
  GENERATOR
    loom::py::loom::gen::amdgpu_contract_fragment
  ARGS
    "--contract-fragment=amdgpu.integer"
  DEPS
    loom::target::contract
)
endif()

if(LOOM_TARGET_AMDGPU)
loom_target_table_cc_library(
  NAME
    integer_lower_rules
  GENERATOR
    loom::py::loom::gen::amdgpu_lower_rule_shard
  ARGS
    "--contract-fragment=amdgpu.integer"
  DEPS
    loom::codegen::low::lower
    loom::ops::index
    loom::ops::scalar
    loom::target::arch::amdgpu::lower_materializers
)
endif()

if(LOOM_TARGET_AMDGPU)
loom_target_table_cc_library(
  NAME
    matrix
  GENERATOR
    loom::py::loom::gen::amdgpu_contract_fragment
  ARGS
    "--contract-fragment=amdgpu.matrix"
  DEPS
    loom::target::contract
)
endif()

if(LOOM_TARGET_AMDGPU)
loom_target_table_cc_library(
  NAME
    reduce
  GENERATOR
    loom::py::loom::gen::amdgpu_contract_fragment
  ARGS
    "--contract-fragment=amdgpu.reduce"
  DEPS
    loom::target::contract
)
endif()

if(LOOM_TARGET_AMDGPU)
loom_target_table_cc_library(
  NAME
    reduce_lower_rules
  GENERATOR
    loom::py::loom::gen::amdgpu_lower_rule_shard
  ARGS
    "--contract-fragment=amdgpu.reduce"
  DEPS
    loom::codegen::low::lower
    loom::ops::vector
    loom::target::arch::amdgpu::lower_kinds
    loom::target::arch::amdgpu::lower_materializers
)
endif()

if(LOOM_TARGET_AMDGPU)
loom_target_table_cc_library(
  NAME
    view
  GENERATOR
    loom::py::loom::gen::amdgpu_contract_fragment
  ARGS
    "--contract-fragment=amdgpu.view"
  DEPS
    loom::target::contract
)
endif()

if(LOOM_TARGET_AMDGPU)
loom_target_table_cc_library(
  NAME
    view_lower_rules
  GENERATOR
    loom::py::loom::gen::amdgpu_lower_rule_shard
  ARGS
    "--contract-fragment=amdgpu.view"
  DEPS
    loom::codegen::low::lower
    loom::ops::view
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
