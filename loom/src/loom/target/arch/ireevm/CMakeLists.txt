################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/src/loom/target/arch/ireevm/BUILD.bazel                                 #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(LOOM_TARGET_ARCH_IREE_VM)
loom_low_descriptor_cc_library(
  NAME
    descriptors
  GENERATOR
    loom::py::loom::gen::ireevm_low_descriptor_shard
  ARGS
    "--descriptor-set=ireevm_core"
  DEPS
    loom::codegen::low::descriptors
)
endif()

if(LOOM_TARGET_ARCH_IREE_VM)
loom_cc_library(
  NAME
    target_records
  HDRS
    "feature_bits.h"
    "target_records.h"
  SRCS
    "target_records.c"
  DEPS
    iree::base
    loom::target::types
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_IREE_VM)
loom_generated_cc_library(
  NAME
    ops
  GENERATOR
    loom::py::loom::gen::c_tables_generator
  SRCS
    "ops/registry.c"
    "ops/verify.c"
  GENERATED_SRC_FLAGS
    "--builders"
    "--tables"
  GENERATED_SRCS
    "ops/builders.c"
    "ops/tables.c"
  HDRS
    "ops/ops.h"
    "ops/registry.h"
  ARGS
    "--dialect=ireevm"
  DEPS
    ::target_records
    iree::base
    loom::error::emitter
    loom::error::error_defs
    loom::ir
    loom::ops::func_symbol_facts
    loom::ops::function_contract_verify
    loom::ops::op_defs
    loom::ops::target
    loom::target::types
)
endif()

if(LOOM_TARGET_ARCH_IREE_VM)
loom_cc_library(
  NAME
    lower
  HDRS
    "lower.h"
  SRCS
    "lower/registry.c"
  DEPS
    ::descriptors_ids
    ::ops
    iree::base
    loom::codegen::low::lower
    loom::error::error_defs
    loom::ir
    loom::ops::low
    loom::ops::scalar
    loom::target::arch::ireevm::contracts::core
    loom::target::arch::ireevm::contracts::core_lower_rules
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_IREE_VM)
loom_cc_library(
  NAME
    low_registry
  HDRS
    "low_registry.h"
  SRCS
    "low_registry.c"
  DEPS
    ::descriptors
    ::target_records
    iree::base
    loom::codegen::low::descriptors
    loom::target::low_descriptor_registry
    loom::target::types
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_IREE_VM)
loom_cc_library(
  NAME
    math_policy
  HDRS
    "math_policy.h"
  TEXTUAL_HDRS
    "math_policy_tables.inl"
  SRCS
    "math_policy.c"
  DEPS
    iree::base
    loom::target::math_policy
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_IREE_VM)
loom_cc_library(
  NAME
    ref_lifetime_pass
  HDRS
    "ref_lifetime_pass.h"
  SRCS
    "ref_lifetime_pass.c"
  DEPS
    ::ops
    iree::base
    loom::analysis::ownership_lifetime
    loom::ir
    loom::pass::types
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_IREE_VM)
loom_cc_library(
  NAME
    pass_registry
  HDRS
    "pass_registry.h"
  SRCS
    "pass_registry.c"
  DEPS
    ::ref_lifetime_pass
    iree::base
    loom::pass::registry
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_IREE_VM)
loom_cc_library(
  NAME
    provider
  HDRS
    "provider.h"
  SRCS
    "provider.c"
  DEPS
    ::low_registry
    ::lower
    ::math_policy
    ::ops
    ::pass_registry
    iree::base
    loom::pass::builder
    loom::target::provider
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_IREE_VM)
loom_cc_library(
  NAME
    check_provider
  HDRS
    "check/provider.h"
  SRCS
    "check/provider.c"
  DEPS
    ::provider
    iree::base
    loom::tools::loom-check::provider
  TESTONLY
  PUBLIC
)
endif()

if(LOOM_TARGET_ARCH_IREE_VM)
iree_native_test(
  NAME
    "test_import_decl"
  ARGS
    "${CMAKE_CURRENT_SOURCE_DIR}/test/import_decl.loom-test"
  SRC
    loom::tools::loom-check::full::loom-check
  LABELS
    "loom-check"
)
endif()

if(LOOM_TARGET_ARCH_IREE_VM)
iree_native_test(
  NAME
    "test_import_decl_diagnostics"
  ARGS
    "${CMAKE_CURRENT_SOURCE_DIR}/test/import_decl_diagnostics.loom-test"
  SRC
    loom::tools::loom-check::full::loom-check
  LABELS
    "loom-check"
)
endif()

if(LOOM_TARGET_ARCH_IREE_VM)
iree_native_test(
  NAME
    "test_ref_lifetime"
  ARGS
    "${CMAKE_CURRENT_SOURCE_DIR}/test/ref_lifetime.loom-test"
  SRC
    loom::tools::loom-check::full::loom-check
  LABELS
    "loom-check"
)
endif()

if(LOOM_TARGET_ARCH_IREE_VM)
iree_native_test(
  NAME
    "allocation"
  ARGS
    "${CMAKE_CURRENT_SOURCE_DIR}/test/source_low/allocation.loom-test"
  SRC
    loom::tools::loom-check::full::loom-check
  LABELS
    "loom-check"
)
endif()

if(LOOM_TARGET_ARCH_IREE_VM)
iree_native_test(
  NAME
    "pass_source_low"
  ARGS
    "${CMAKE_CURRENT_SOURCE_DIR}/test/source_low/pass_source_low.loom-test"
  SRC
    loom::tools::loom-check::full::loom-check
  LABELS
    "loom-check"
)
endif()

if(LOOM_TARGET_ARCH_IREE_VM)
iree_native_test(
  NAME
    "source_low"
  ARGS
    "${CMAKE_CURRENT_SOURCE_DIR}/test/source_low/source_low.loom-test"
  SRC
    loom::tools::loom-check::full::loom-check
  LABELS
    "loom-check"
)
endif()

if(LOOM_TARGET_ARCH_IREE_VM)
iree_native_test(
  NAME
    "source_low_diagnostics"
  ARGS
    "${CMAKE_CURRENT_SOURCE_DIR}/test/source_low/source_low_diagnostics.loom-test"
  SRC
    loom::tools::loom-check::full::loom-check
  LABELS
    "loom-check"
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
