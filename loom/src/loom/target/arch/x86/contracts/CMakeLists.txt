################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/src/loom/target/arch/x86/contracts/BUILD.bazel                          #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(LOOM_TARGET_X86)
loom_target_table_cc_library(
  NAME
    scalar
  GENERATOR
    loom::py::loom::gen::x86_contract_fragment
  ARGS
    "--contract-fragment=x86.scalar"
  DEPS
    loom::target::contract
)
endif()

if(LOOM_TARGET_X86)
loom_target_table_cc_library(
  NAME
    scalar_lower_rules
  GENERATOR
    loom::py::loom::gen::x86_lower_rule_shard
  ARGS
    "--contract-fragment=x86.scalar"
  DEPS
    loom::codegen::low::lower
    loom::ops::buffer
    loom::ops::index
    loom::ops::scalar
    loom::ops::view
)
endif()

if(LOOM_TARGET_X86)
loom_target_table_cc_library(
  NAME
    avx2
  GENERATOR
    loom::py::loom::gen::x86_contract_fragment
  ARGS
    "--contract-fragment=x86.avx2"
  DEPS
    loom::target::contract
)
endif()

if(LOOM_TARGET_X86)
loom_target_table_cc_library(
  NAME
    avx2_lower_rules
  GENERATOR
    loom::py::loom::gen::x86_lower_rule_shard
  ARGS
    "--contract-fragment=x86.avx2"
  DEPS
    loom::codegen::low::lower
    loom::ops::buffer
    loom::ops::index
    loom::ops::scalar
    loom::ops::vector
    loom::ops::view
)
endif()

if(LOOM_TARGET_X86)
loom_target_table_cc_library(
  NAME
    avx512
  GENERATOR
    loom::py::loom::gen::x86_contract_fragment
  ARGS
    "--contract-fragment=x86.avx512"
  DEPS
    loom::target::contract
)
endif()

if(LOOM_TARGET_X86)
loom_target_table_cc_library(
  NAME
    avx512_lower_rules
  GENERATOR
    loom::py::loom::gen::x86_lower_rule_shard
  ARGS
    "--contract-fragment=x86.avx512"
  DEPS
    loom::codegen::low::lower
    loom::ops::buffer
    loom::ops::index
    loom::ops::scalar
    loom::ops::vector
    loom::ops::view
)
endif()

if(LOOM_TARGET_X86)
loom_target_table_cc_library(
  NAME
    packed_dot
  GENERATOR
    loom::py::loom::gen::x86_contract_fragment
  ARGS
    "--contract-fragment=x86.packed_dot"
  DEPS
    loom::target::contract
)
endif()

if(LOOM_TARGET_X86)
loom_target_table_cc_library(
  NAME
    packed_dot_lower_rules
  GENERATOR
    loom::py::loom::gen::x86_lower_rule_shard
  ARGS
    "--contract-fragment=x86.packed_dot"
  DEPS
    loom::codegen::low::lower
    loom::ops::vector
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
