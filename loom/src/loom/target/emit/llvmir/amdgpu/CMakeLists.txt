################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# loom/src/loom/target/emit/llvmir/amdgpu/BUILD.bazel                          #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

if(LOOM_EMIT_LLVMIR)
loom_cc_library(
  NAME
    intrinsics
  HDRS
    "intrinsics.h"
  SRCS
    "intrinsics.c"
  DEPS
    iree::base
    loom::target::emit::llvmir::core
  PUBLIC
)
endif()

if(LOOM_EMIT_LLVMIR)
loom_cc_library(
  NAME
    lower
  HDRS
    "lower.h"
  SRCS
    "lower/registry.c"
  DEPS
    ::intrinsics
    iree::base
    loom::ops::llvmir
    loom::target::emit::llvmir::lower
  PUBLIC
)
endif()

if(LOOM_EMIT_LLVMIR)
loom_cc_library(
  NAME
    legality
  HDRS
    "legality.h"
  SRCS
    "legality.c"
  DEPS
    iree::base
    loom::ops::llvmir
    loom::target::emit::llvmir::legality
  PUBLIC
)
endif()

if(LOOM_EMIT_LLVMIR)
loom_cc_library(
  NAME
    target_env
  HDRS
    "target_env.h"
  SRCS
    "target_env.c"
  DEPS
    iree::base
    loom::target::arch::amdgpu::target_info_defs
    loom::target::emit::llvmir::target_presets
    loom::target::types
  PUBLIC
)
endif()

if(LOOM_EMIT_LLVMIR)
loom_cc_test(
  NAME
    legality_test
  SRCS
    "legality_test.cc"
  DEPS
    ::legality
    ::target_env
    iree::base::internal::arena
    iree::testing::gtest
    iree::testing::gtest_main
    loom::ir
    loom::ops::func
    loom::ops::llvmir
    loom::target::emit::llvmir::legality
  LABELS
    "iree-build-requirement=loom.emit.llvmir"
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
