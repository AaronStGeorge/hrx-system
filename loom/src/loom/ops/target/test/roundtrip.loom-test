// RUN: roundtrip

// Generic target record.
target.generic<reference> @reference

target.generic<reference> @host {subgroup_size = 0}

target.artifact @host_object target(@host) {abi = object_file, artifact_format = elf}
