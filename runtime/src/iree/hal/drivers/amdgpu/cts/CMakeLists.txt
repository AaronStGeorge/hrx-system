################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# runtime/src/iree/hal/drivers/amdgpu/cts/BUILD.bazel                          #
#                                                                              #
# Add CMake-only content below the preserve marker at the end of this file.    #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_amdgpu_expand_target_selectors(
  _IREE_HAL_AMDGPU_EXACT_TARGETS
  "${IREE_AMDGPU_TARGET_EXPANSION_EXACT}"
  ${IREE_HAL_AMDGPU_TARGETS}
)
if(NOT _IREE_HAL_AMDGPU_EXACT_TARGETS)
  return()
endif()

iree_add_all_subdirs()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_library(
  NAME
    backends
  SRCS
    "backends.cc"
  DEPS
    iree::hal
    iree::hal::cts::util::registry
    iree::hal::drivers::amdgpu::registration
  TESTONLY
  ALWAYSLINK
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_amdgpu_hal_cts_testdata(
  NAME
    testdata_amdgpu
  TARGET
    amdgcn-amd-amdhsa
  TARGETS
    ${IREE_HAL_AMDGPU_TARGETS}
  FORMAT_NAME
    amdgpu
  FORMAT_STRING
    [=["amdgcn-amd-amdhsa--{AMDGPU_TARGET}"]=]
  IDENTIFIER
    "iree_cts_testdata_amdgpu"
  BACKEND_NAME
    "amdgpu"
  INTERNAL_HDRS
    "${PROJECT_BINARY_DIR}/runtime/src/iree/hal/drivers/amdgpu/abi/amdgpu_abi_bitcode_hdrs.stamp"
    "${PROJECT_BINARY_DIR}/runtime/src/iree/hal/drivers/amdgpu/device/bitcode_hdrs.stamp"
  SRCS
    "testdata/command_buffer_dispatch_constants_bindings_test.c"
    "testdata/command_buffer_dispatch_constants_test.c"
    "testdata/command_buffer_dispatch_indirect_parameters_test.c"
    "testdata/command_buffer_dispatch_multi_entrypoint_test.c"
    "testdata/command_buffer_dispatch_multi_workgroup_test.c"
    "testdata/command_buffer_dispatch_test.c"
    "testdata/executable_cache_test.c"
    "testdata/executable_test.c"
  TESTONLY
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_amdgpu_library_variants(
  NAME
    asanrtl_test_runtime
  TARGET
    amdgcn-amd-amdhsa
  TARGETS
    ${IREE_HAL_AMDGPU_TARGETS}
  INTERNAL_HDRS
    "${PROJECT_BINARY_DIR}/runtime/src/iree/hal/drivers/amdgpu/abi/amdgpu_abi_bitcode_hdrs.stamp"
    "${PROJECT_BINARY_DIR}/runtime/src/iree/hal/drivers/amdgpu/device/bitcode_hdrs.stamp"
  SRCS
    "testdata/asanrtl_test_runtime.c"
  TESTONLY
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_amdgpu_hal_cts_testdata(
  NAME
    asan_testdata_amdgpu
  TARGET
    amdgcn-amd-amdhsa
  TARGETS
    ${IREE_HAL_AMDGPU_TARGETS}
  FORMAT_NAME
    amdgpu_asan
  FORMAT_STRING
    [=["amdgcn-amd-amdhsa--{AMDGPU_TARGET}"]=]
  IDENTIFIER
    "iree_cts_asan_testdata_amdgpu"
  BACKEND_NAME
    "amdgpu"
  INTERNAL_HDRS
    "${PROJECT_BINARY_DIR}/runtime/src/iree/hal/drivers/amdgpu/abi/amdgpu_abi_bitcode_hdrs.stamp"
    "${PROJECT_BINARY_DIR}/runtime/src/iree/hal/drivers/amdgpu/device/bitcode_hdrs.stamp"
  SRCS
    "testdata/manual_asan_noop_test.c"
    "testdata/manual_asan_test.c"
  DEPS
    ::asanrtl_test_runtime_{AMDGPU_CODE_OBJECT_TARGET_FRAGMENT}
  NO_INTERNALIZE
  TESTONLY
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_library(
  NAME
    asan_executable_test_util
  HDRS
    "asan_executable_test_util.h"
  DEPS
    iree::base::threading::thread
    iree::hal
    iree::hal::cts::util::test_base
    iree::hal::drivers::amdgpu::util::libhsa
    iree::hal::drivers::amdgpu::util::target_id
    iree::hal::drivers::amdgpu::util::topology
  TESTONLY
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_library(
  NAME
    asan_executable_test
  SRCS
    "asan_executable_test.cc"
  DEPS
    ::asan_executable_test_util
    iree::hal::drivers::amdgpu::abi
    iree::hal::drivers::amdgpu::headers
  TESTONLY
  ALWAYSLINK
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    asan_executable_tests
  SRCS
    "${PROJECT_SOURCE_DIR}/runtime/src/iree/hal/cts/util/test_main.cc"
  DEPS
    ::asan_executable_test
    ::backends
    ::testdata_amdgpu
    iree::base::tooling::flags
    iree::hal::cts::util::registry
    iree::hal::cts::util::test_base
    iree::testing::gtest
  ARGS
    "--amdgpu_asan=true"
  LABELS
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_library(
  NAME
    manual_asan_executable_test
  SRCS
    "manual_asan_executable_test.cc"
  DEPS
    ::asan_executable_test_util
    iree::hal
    iree::hal::cts::util::registry
    iree::hal::cts::util::test_base
    iree::hal::drivers::amdgpu::buffer
  TESTONLY
  ALWAYSLINK
  PUBLIC
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_cc_test(
  NAME
    manual_asan_executable_tests
  SRCS
    "${PROJECT_SOURCE_DIR}/runtime/src/iree/hal/cts/util/test_main.cc"
  DEPS
    ::asan_testdata_amdgpu
    ::backends
    ::manual_asan_executable_test
    iree::base::tooling::flags
    iree::hal::cts::util::registry
    iree::hal::cts::util::test_base
    iree::testing::gtest
  ARGS
    "--amdgpu_asan=true"
  LABELS
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

if(IREE_HAL_DRIVER_AMDGPU)
iree_hal_cts_test_suite(
  BACKENDS_LIB
    ::backends
  TESTDATA_LIBS
    ::testdata_amdgpu
  LABELS
    "iree-build-requirement=runtime.hal.amdgpu"
    "iree-run-requirement=runtime.resource.amd_gpu"
    "runtime-resource=amd-gpu"
  RESOURCE_GROUP
    iree-hal-drivers-amdgpu-tests
)
endif()

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
